{
  "name": "boba-cat",
  "version": "2.2.0",
  "description": "simple cat enjoying some boba",
  "private": true,
  "scripts": {
    "start": "cd src && tauri dev",
    "build": "cd src && tauri build",
    "package": "cd src && tauri build",
    "make": "cd src && tauri build"
  },
  "author": "lyova24",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
